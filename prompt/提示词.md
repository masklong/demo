# 提示词

## 一、UI 生成

### 步骤一 框架生成

根据图片生成大屏页面，要求如下：

1. 合理将大屏中拆分组件，每个模块的位置要和图片中一致
2. 每个模块的组件的样式、布局、颜色等要符合图片中的样式。注意每个模块之间的间距
3. 考虑大屏的适配性，在不同大屏比例下的适配问题，采用当前主流方案解决
4. 图表使用 echarts 实现
5. 页面的图标、图片可以先使用开源图库解决，后续我自己替换

在 XXX 中实现

6. 数据源使用 mock 数据
   > 建议一次不设置太多任务，分多次生成

### 步骤二 细节完善

1. 第一次
   根据 figma 的设计内容，重新实现 设备情况板块的样式。figma 设计内容：
   补充信息：设计稿中的图表是一个环形比例图，对应在线设备、离线设备、故障设备的比例，请使用 echarts 实现

   根据 Figma 设计实现 [组件名称]
   设计链接：[Figma URL]
   技术要求：

- 使用 ECharts 实现设计稿中的图表。图表是一个环形比例图，对应在线设备、离线设备、故障设备的比例，总比例为 100%，mock 的数据要符合总比例为 100%。因此可以不用关注 figma 设计中图表的图片信息，只关注图表的样式和布局。
- 样式使用 Tailwind/Less
- 保持与设计图完全一致
- 图表背景色使用#14142b。
- 设备总数元素的位置在圆环的中间。

（补充：当在使用 Claude code 过程中，想起需要加规则，可以随时补充）

2. 饼图
   设备情况组件中，上面的图表是一个环形比例图，对应在线设备、离线设备、故障设备的比例,总比例为 100%，mock 的数据要符合总比例为 100%，请使用 echarts 优化当前 @src/pages/BigScreen/components/DeviceStatus/index.tsx
   的圆环效果。保持和图片中一致。echarts 图表背景色使用#14142b。设备总数元素的位置在圆环的中间。

> 补充：反问 AI 哪些提示词是优秀的，对它完成任务有帮助，然后反写到 rules 中或者 Claude 中。
> 以上对话中，哪些提示词对你有较重的影响？提炼一下，方便我以后提示你

## 二、mock 数据介绍

政务内网 vue2 老项目，没有接口类型定义。我是定义了一个 subagent ，让它去找接口在哪调用的，使用了哪些字段，然后反推接口字段形成 mock 数据。

## 三、skill 演示

不使用 skill:

```
在 @src/pages/skill/ 中实现图片中的内容。样式要求：重新设计整体风格，要求更现代化的OA系统。只用关注图片中的布局及内容即可，不要收到图片中的样式影响
```

使用 skill：
